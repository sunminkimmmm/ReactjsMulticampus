let base = 100;
const add = (x) => {
    return base + x;
}
const multiply = (x) => {
    return base * x;
}
const substract = (x) => {
    return base - x;
}

export { add, multiply };
export default substract;